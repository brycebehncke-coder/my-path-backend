{
  "name": "my-path-backend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node --env-file=.env server.mjs",
    "start": "node server.mjs",
    "test": "node --test server.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
